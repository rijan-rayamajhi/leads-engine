# Neon Postgres (same DB the crawler writes to)
DATABASE_URL=postgres://...
# Signs the session cookie: openssl rand -hex 32
AUTH_SECRET=
# Bootstrap admin, email -> password. Ignored once the users table has a row.
USERS={"you@company.com":"pick-a-real-one"}
